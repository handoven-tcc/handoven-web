import { Outlet } from "react-router-dom";

const Pages = () => {
  return (
    <>
      {/* <Header></Header> */}
      <Outlet></Outlet>
      {/* <Footer></Footer> */}
    </>
  );
};

export default Pages;
